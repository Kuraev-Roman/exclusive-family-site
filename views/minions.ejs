<%- include('partials/header') %>

<div class="wrap">
  <div class="eyebrow">Для младших членов семьи</div>
  <h1 class="gradient-text" style="font-size:2.1rem;">Приколы про миньонов 🍌</h1>
  <p style="color:var(--muted); margin-bottom:30px;">Немного жёлтого юмора в золото-чёрной семье.</p>

  <div class="grid grid-3">
    <% jokes.forEach(function(j) { %>
      <div class="minion-card"><%= j.text %></div>
    <% }) %>
  </div>
</div>

<%- include('partials/footer') %>
